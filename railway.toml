[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
# Next は 0.0.0.0 で待ち受け（Railway のヘルスチェック用）
startCommand = "npx prisma migrate deploy && npx next start --hostname 0.0.0.0"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/"
healthcheckTimeout = 30
